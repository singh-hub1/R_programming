#Try any five built-in mathematical functions in R programming. and explain each in your own way

x<- -9
print(abs(x)) #It returns the absolute value of x

y<-144
print(sqrt(y))#It returns the square root of value of y

z<-8.9
print(ceiling(z))#It returns the value which after the decimal point

a<-8.9
print(floor(a))#It returns the value which before the decimal point

b<-12
print(log(b))#It returns the log value of 12 with base of 10.